name: ZEVANORY voice naturality evidence gate
on:
  pull_request:
  push:
    branches: [gh-pages]
  workflow_dispatch:
permissions:
  contents: read
jobs:
  voice-naturality:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Reject false naturality certification
        run: python3 scripts/voice_naturality_gate.py
