name = "zevanory-git-reproducible"
compatibility_date = "2026-09-04"

# Mirrors the current production runtime while the API Worker source is
# reconciled independently. No compatibility flags are configured.
[assets]
directory = "."
binding = "ATIVOS"
not_found_handling = "404-page"
html_handling = "auto-trailing-slash"

[vars]
ZEVANORY_PUBLIC_SALES = "false"
CHECKOUT_ENABLED = "false"
FINANCIAL_EVENTS_ENABLED = "false"
PRE_SALE_GATES_APPROVED = "false"
ABSOLUTE_RELEASE_APPROVED = "false"
