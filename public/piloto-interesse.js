const form=document.getElementById('interest-form');const button=document.getElementById('submit');const status=document.getElementById('status');
form.addEventListener('submit',async(event)=>{event.preventDefault();button.disabled=true;status.textContent='Registrando consentimento...';try{const response=await fetch('/api/pilot-interest',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({name:document.getElementById('name').value,email:document.getElementById('email').value,company_website:document.getElementById('company_website').value,consent:document.getElementById('consent').checked,privacy_accepted:document.getElementById('privacy').checked})});const data=await response.json().catch(()=>({}));if(!response.ok)throw new Error(data.error||'request_failed');form.reset();status.textContent='Interesse registrado. Se houver vaga, a ZEVANORY poderá enviar um convite individual para o piloto.';}catch{status.textContent='Não foi possível registrar agora. Nenhuma cobrança foi criada.';}finally{button.disabled=false;}});
