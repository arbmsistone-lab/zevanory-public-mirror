import test from 'node:test';
import assert from 'node:assert/strict';
import {readFile} from 'node:fs/promises';
import {createYouTubeIdentityOAuthStart,readYouTubeIdentityOAuthCookie,exchangeYouTubeIdentityCode,fetchYouTubeIdentity,updateYouTubeCanonicalDescription} from '../src/youtubeIdentityOAuth.mjs';
const key=Buffer.alloc(32,7).toString('base64');
const env={YOUTUBE_OAUTH_CLIENT_ID:'client.apps.googleusercontent.com',YOUTUBE_OAUTH_CLIENT_SECRET:'secret',COMMERCIAL_OAUTH_ENCRYPTION_KEY:key};
test('YouTube identity OAuth requests only channel edit scope and offline consent',()=>{const s=createYouTubeIdentityOAuthStart(env);const u=new URL(s.url);assert.equal(u.searchParams.get('scope'),'https://www.googleapis.com/auth/youtube.force-ssl');assert.equal(u.searchParams.get('access_type'),'offline');assert.equal(u.searchParams.get('prompt'),'consent');assert.equal(u.searchParams.get('redirect_uri'),'https://zevanory.api.br/api/oauth/youtube/callback');readYouTubeIdentityOAuthCookie(s.cookie,s.state,env);});
test('YouTube identity OAuth rejects invalid state',()=>{const s=createYouTubeIdentityOAuthStart(env);assert.throws(()=>readYouTubeIdentityOAuthCookie(s.cookie,'wrong',env),/state_invalid/);});
test('YouTube identity exchange requires refresh token and edit scope',async()=>{const fetchImpl=async()=>new Response(JSON.stringify({access_token:'a',refresh_token:'r',expires_in:3600,token_type:'Bearer',scope:'https://www.googleapis.com/auth/youtube.force-ssl'}),{status:200});const t=await exchangeYouTubeIdentityCode({code:'c',env,fetchImpl});assert.equal(t.refresh_token,'r');});
test('YouTube identity refuses a different channel',async()=>{const fetchImpl=async()=>new Response(JSON.stringify({items:[{id:'WRONG'}]}),{status:200});await assert.rejects(()=>fetchYouTubeIdentity('a',fetchImpl),/identity_mismatch/);});test('YouTube description update preserves existing branding settings',async()=>{let sent;const fetchImpl=async(_u,o)=>{sent=JSON.parse(o.body);return new Response(JSON.stringify({id:sent.id,brandingSettings:sent.brandingSettings}),{status:200})};const channel={id:'UCMl8-SxMVv77S2tz2H63P3A',brandingSettings:{channel:{keywords:'zevanory ai',country:'BR',description:'old'},image:{bannerExternalUrl:'x'}}};await updateYouTubeCanonicalDescription({accessToken:'a',channel,description:'canonical',fetchImpl});assert.equal(sent.brandingSettings.channel.description,'canonical');assert.equal(sent.brandingSettings.channel.keywords,'zevanory ai');assert.equal(sent.brandingSettings.channel.country,'BR');assert.equal(sent.brandingSettings.image.bannerExternalUrl,'x');});
test('migration and Vercel routes include youtube_identity provider',async()=>{const m=await readFile(new URL('../db/migrations/023_youtube_identity_oauth.sql',import.meta.url),'utf8');const v=JSON.parse(await readFile(new URL('../vercel.json',import.meta.url),'utf8'));assert.match(m,/youtube_identity/);assert.ok(v.rewrites.some(x=>x.source==='/api/oauth/youtube/start'));assert.ok(v.rewrites.some(x=>x.source==='/api/oauth/youtube/callback'));});