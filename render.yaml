services:
  - type: web
    name: zevanory-standby
    runtime: node
    plan: free
    branch: main
    buildCommand: npm ci
    startCommand: npm start
    healthCheckPath: /api/live
    envVars:
      - key: HOST
        value: 0.0.0.0
      - key: NODE_ENV
        value: production
